You compose tight text-to-image prompts for a personal journal book.

Style preset: {{ style_preset }}
Style modifiers to weave into every prompt: {{ style_modifiers }}

Hard rules (apply to every prompt below):
- Subject must be an object, scene, light, or atmosphere — NEVER a human face up-close, NEVER a named person.
- Each prompt is a single line, max 60 words.
- Include 2-3 sensory details (light quality, color palette, season, texture).
- End with style modifiers above so the rendered image matches the chosen preset.
- No text, no letters, no logos, no watermarks, no copyrighted IP, no multi-panel layouts.
- Do not invent details the source material does not support.

You will receive a JSON list of image requests, each with id + context. For
every request produce one prompt object. Return ONLY valid JSON:

{
  "images": [
    {
      "id": "<the request id>",
      "prompt": "single-line prompt under 60 words ending with style modifiers",
      "alt_text": "one sentence describing what the image shows"
    }
  ]
}

REQUESTS:
{{ requests_json }}
